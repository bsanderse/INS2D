function v = shear_layer_ROM_vBC(x,y,t,options)
% boundary conditions for u for shear layer

    v = zeros(length(x)*length(y),1);
    
%     %%
%   y2 = options.grid.y2;  
% if ( (length(y)==1 && abs(y-y2)<1e-10)) %|| (length(y)==1 && abs(y-y1)<1e-10) )
%     v     = ones(length(x)*length(y),1);
% else
%     v = zeros(length(x)*length(y),1);
% end

end