function u = shear_layer_ROM_uBC(x,y,t,options)
% boundary conditions for u for shear layer

    u  = zeros(length(x)*length(y),1);
 
% y2 = options.grid.y2;
% 
% if ( (length(y)==1 && abs(y-y2)<1e-10)) %|| (length(y)==1 && abs(y-y1)<1e-10) )
%     u     = ones(length(x)*length(y),1);
% else
%     u = zeros(length(x)*length(y),1);
% end


end